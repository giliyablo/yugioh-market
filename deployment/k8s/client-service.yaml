apiVersion: v1
kind: Service
metadata:
  name: tcg-market-client
  namespace: tcg-market
  labels:
    app: tcg-market-client
spec:
  selector:
    app: tcg-market-client
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
