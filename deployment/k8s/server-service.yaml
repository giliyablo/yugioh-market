apiVersion: v1
kind: Service
metadata:
  name: tcg-market-server
  namespace: tcg-market
  labels:
    app: tcg-market-server
spec:
  selector:
    app: tcg-market-server
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
  type: ClusterIP
